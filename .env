# Ollama
OLLAMA_MODEL=qwen2.5-coder:7b
#OLLAMA_HOST=localhost
#OLLAMA_PORT=11434

# Services that can be restarted via /restart, /fix, and restart_service tool.
# Comma-separated. Each entry MUST have a matching NOPASSWD rule in sudoers.
ALLOWED_RESTART_SERVICES=ollama
